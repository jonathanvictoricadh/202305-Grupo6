server:
  port: ${PORT:8888}

SPRING:
  APPLICATION:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https:///github
          default-label: clase-2-ConfigServer
